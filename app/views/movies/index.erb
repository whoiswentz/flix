<% @movies.each do |movie| %>
  <li><%= movie.title %> (<%= movie.rating %>): <%= format_gross(movie) %></li>
  <p><%= truncate(movie.description, length: 40, separator: ' ') %></p>
  <p><%= year_of(movie) %></p>
<% end %>